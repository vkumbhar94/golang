package main

import (
	"fmt"
	"math/big"
)

func main() {
	b := big.Int{}
	b.SetBit(&b, 10, 1)
	b.SetBit(&b, 5, 1)
	fmt.Println(b)
	fmt.Println(b.Bit(10))
	fmt.Println(b.Bit(1))
	fmt.Println(b.Bit(5))
}
